package trl8
/*
import org.specs._
import org.specs.runner.{ConsoleRunner, JUnit4}

class SearchSpecTest extends JUnit4(SearcherSpec)
//class SearcherSuite extends ScalaTestSuite(Searcher)
object SearcherSpecRunner extends ConsoleRunner(SearcherSpec)

object SearcherSpec extends Specification {
  "Searcher" should {
    "Do a twitter search on #trl8" in {
      val list = Nil
      list must beEmpty
    }
  }
}

*/